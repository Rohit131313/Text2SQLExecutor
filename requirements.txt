streamlit
google-generativeai
python-dotenv
langchain
PyPDF2
chromadb
faiss-cpu
pdf2image
# sqlite3 # sqlite3 is included in the Python standard library, so it doesn't need to be installed separately via pip.

